--index-url https://mirrors.huaweicloud.com/repository/pypi/simple

# 邮件工作总结汇总程序依赖包
# Email Work Summary Tool Dependencies

# 编码检测库 - 用于自动检测邮件文件编码
chardet>=5.0.0

# 环境变量管理 - 从 .env 文件加载邮箱配置
python-dotenv>=1.0.0

# 路径处理库 (Python 3.4+ 内置，但为了兼容性列出)
# pathlib - 已内置于Python 3.4+

# 其他内置库（无需安装）:
# - email: 邮件解析
# - datetime: 日期时间处理  
# - re: 正则表达式
# - logging: 日志记录
# - argparse: 命令行参数解析
# - collections: 数据结构
# - typing: 类型提示

# Web 服务依赖
flask>=3.0.0
markdown>=3.5.0
gunicorn>=21.2.0

# MySQL 数据库依赖
PyMySQL>=1.1.0
DBUtils>=3.0.0
cryptography>=41.0.0

# 2FA 动态密码系统依赖
pyotp>=2.9.0
